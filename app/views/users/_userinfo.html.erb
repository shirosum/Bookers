		<% if book.errors.any? %>
		  <div id="error_explanation">
		 	<h2><%= book.errors.count %> error prohibited this obj from being saved:</h2>
		 	<ul>
		 		<% book.errors.full_messages.each do |message| %>
		 		<li><%= message %></li>
		 		<% end %>
		 	</ul>
		  </div>
		<% end %>

			<div class="col-xs-3">
				<h2 class="">User info</h2>
				<h4 class=""><%= attachment_image_tag user, :image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg", class: "" %></h4>
				<table class="table">
					<tbody class="">
						<tr class="">
							<td class="">name</td>
							<td class=""><%= user.name %></td>
						</tr>
						<tr>
							<td class="">introduction</td>
							<td class=""><%= user.body %></td>
						</tr>
					</tbody>
				</table>

				<div class="row">
			      <%= link_to "", edit_user_path(user), class: "edit_user_#{user.id} col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>
				</div>

				<h2>New Book</h2>
			    <%= form_for (book) do |f| %>
				  <div class="field row">
					<label class="book_title">Title</label>
					<%= f.text_field :title, class: "col-xs-12 book_title", id: "book_title" %>
				  </div>
				  <div class="field row">
					<label class="book_body">Opinion</label>
				    <%= f.text_area :body, class: "col-xs-12 book_body", id: "book_body" %>
				  </div>
				  <div class="actions row">
					<%= f.submit "Create Book", class: "col-xs-12 btn btn-primary" %>
		          </div>
			    <% end %>
			</div>
