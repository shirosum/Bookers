			<div class="col-xs-9">
			  <h2>User info</h2>
				<%= form_for(@user) do |f| %>
				  <% if @user.errors.any? %>
				    <div id="error_explanation">
				      <h2><%= @user.errors.count %> error prohibited this obj from being saved:</h2>
				      <ul>
				      	<% @user.errors.full_messages.each do |message| %>
				    	<li><%= message %></li>
				    	<% end %>
				      </ul>
				    </div>
				  <% end %>
				  <div class="field row">
				   <div class="col-xs-3">
				    <label class="user_name">Name</label>
				    <%= f.text_field :name, class: "name col-xs-12" %>
				   </div>
				  </div>
				  <div class="field row">
				   <div class="col-xs-3">
				    <label class="user_image">Image</label>
				    <%= f.attachment_field :image, class: "user_image" %>
				   </div>
				  </div>
				  <div class="field row">
				   <div class="col-xs-6">
			        <label class="user_introduction">introduction</label>
			        <%= f.text_area :body, class: "introduction col-xs-12" %>
			       </div>
			      </div>
			      <div class="actions row">
			       <div class="col-xs-3">
				    <%= f.submit "Update User" %>
				   </div>
				  </div>
				<% end %>
			</div>